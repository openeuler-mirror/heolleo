[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "eulerinstall"
version = "3.0.9"
description = "EulerOS installer - guided, templates etc."
authors = [
    {name = "Anton Hvornum", email = "anton@hvornum.se"},
]
license = {text = "GPL-3.0-only"}
readme = "README.md"
requires-python = ">=3.11"
keywords = ["linux", "euler", "eulerinstall", "installer"]
classifiers = [
    "Programming Language :: Python :: 3.12",
    "Operating System :: POSIX :: Linux",
]
dependencies = [
    "pyparted>=3.13.0",
    "pydantic==2.11.7",
    "cryptography>=42.0.2",
    "typing_extensions>=4.12.0",
]

[project.urls]
Home = "https://openeuler.org"
Documentation = "https://eulerinstall.readthedocs.io/"
Source = "https://github.com/openeuler/eulerinstall"

[project.optional-dependencies]
log = ["systemd_python==235"]
dev = [
    "mypy==1.17.1",
    "flake8==7.3.0",
    "pre-commit==4.2.0",
    "ruff==0.12.7",
    "pylint==3.3.7",
    "pylint-pydantic==0.3.5",
    "pytest==8.4.1",
]
doc = ["sphinx"]

[project.scripts]
eulerinstall = "eulerinstall:run_as_a_module"

[tool.setuptools.dynamic]
readme = {file = ["README.rst", "USAGE.rst"]}

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
# We could specify locales/languages.json etc instead, but catchall works too.
"eulerinstall" = [
    "**/*.py",
    "**/*.mo",
    "**/*.po",
    "**/*.pot",
    "**/*.json",
]

[tool.setuptools.package-dir]
eulerinstall = "eulerinstall"

[tool.mypy]
python_version = "3.12"
files = "."
exclude = "^build/"
disallow_any_explicit = false
disallow_any_expr = false
disallow_any_unimported = true
enable_error_code = [
    "deprecated",
    "explicit-override",
    "ignore-without-code",
    "mutable-override",
    "possibly-undefined",
    "redundant-expr",
    "redundant-self",
    "truthy-bool",
    "truthy-iterable",
    "unimported-reveal",
    "unused-awaitable",
]
show_traceback = true
strict = true
warn_unreachable = true

[[tool.mypy.overrides]]
module = "eulerinstall.default_profiles.*"
disallow_any_explicit = true

[[tool.mypy.overrides]]
module = "eulerinstall.examples.*"
disallow_any_explicit = true

[[tool.mypy.overrides]]
module = "eulerinstall.lib.*"
warn_return_any = false

[[tool.mypy.overrides]]
module = "eulerinstall.lib.disk.*"
# 'Any' imports are allowed because pyparted doesn't have type hints
disallow_any_unimported = false

[[tool.mypy.overrides]]
module = "eulerinstall.lib.models.*"
# 'Any' imports are allowed because pyparted doesn't have type hints
disallow_any_unimported = false

[[tool.mypy.overrides]]
module = "eulerinstall.lib.packages"
disallow_any_explicit = true

[[tool.mypy.overrides]]
module = "eulerinstall.lib.utils"
disallow_any_explicit = true

[[tool.mypy.overrides]]
module = [
    "parted",
]
ignore_missing_imports = true

[tool.bandit]
targets = ["eulerinstall"]
exclude = ["/tests"]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-s"
testpaths = ["tests"]

[tool.pylint.main]
ignore-paths = [
    "^build/",
    "^docs/",
]
load-plugins = ["pylint_pydantic"]
persistent = false
py-version = "3.12"
recursive = true

[tool.pylint.format]
indent-string = '\t'
max-line-length = 160

[tool.pylint."messages control"]
disable = [
    "C",
    "R",
    "attribute-defined-outside-init",
    "broad-exception-caught",
    "cell-var-from-loop",
    "dangerous-default-value",
    "fixme",
    "protected-access",
    "raise-missing-from",
    "unspecified-encoding",
    "unused-argument",
]

[tool.pylint.refactoring]
score = false

[tool.pylint.variables]
init-import = true

[tool.codespell]
skip = "*.lock,node_modules,.next,coverage,build,dist"
# 项目术语/专有名词请加入忽略列表；注意：该值留空会导致 codespell 报错

[tool.ruff]
target-version = "py312"
line-length = 160
extend-exclude = ["build", "dist"]

[tool.ruff.format]
indent-style = "tab"
quote-style = "single"
docstring-code-format = true

[tool.ruff.lint]
select = [
    "ASYNC",  # flake8-async
    "B",      # flake8-bugbear
    "C90",    # mccabe
    "COM",    # flake8-commas
    "DTZ",    # flake8-datetimez
    "E",      # pycodestyle errors
    "EXE",    # flake8-executable
    "F",      # Pyflakes
    "FA",     # flake8-future-annotations
    "FLY",    # flynt
    "G",      # flake8-logging-format
    "I",      # isort
    "ICN",    # flake8-import-conventions
    "ISC",    # flake8-implicit-str-concat
    "LOG",    # flake8-logging
    "PGH",    # pygrep-hooks
    "PIE",    # flake8-pie
    "PLC",    # Pylint conventions
    "PLE",    # Pylint errors
    "PLW",    # Pylint warnings
    "PYI",    # flake8-pyi
    "RSE",    # flake8-raise
    "RUF",    # Ruff-specific rules
    "SLOT",   # flake8-slot
    "T10",    # flake8-debugger
    "UP",     # pyupgrade
    "W",      # pycodestyle warnings
    "YTT",    # flake8-2020
]

ignore = [
    "B006",     # mutable-argument-default
    "B008",     # function-call-in-default-argument
    "B904",     # raise-without-from-inside-except
    "B905",     # zip-without-explicit-strict
    "B909",     # loop-iterator-mutation
    "COM812",   # missing-trailing-comma
    "PLC0415",  # import-outside-top-level
    "PLC1901",  # compare-to-empty-string
    "PLW1514",  # unspecified-encoding
    "PLW1641",  # eq-without-hash
    "PLW2901",  # redefined-loop-name
    "RUF005",   # collection-literal-concatenation
    "RUF015",   # unnecessary-iterable-allocation-for-first-element
    "RUF039",   # unraw-re-pattern
    "RUF051",   # if-key-in-dict-del
    "UP037",    # quoted-annotation
    "W191",     # tab-indentation
    "RUF001",   # 中文全角标点（，（）等）被判为模糊字符，属误报
    "RUF002",   # 同上（注释/文档字符串场景）
    "RUF003",   # 同上（字符串场景）
    "UP036",    # 存量大量 outdated-version-block 注释；清理 PR 处理后再收紧
]

[tool.ruff.lint.mccabe]
max-complexity = 40
