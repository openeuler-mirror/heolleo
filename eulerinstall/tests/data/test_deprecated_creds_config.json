{
    "!root-password": "{{ROOT_PASSWORD}}",
    "!users": [
        {
            "!password": "{{USER_PASSWORD}}",
            "sudo": true,
            "username": "user_name",
            "groups": ["wheel"]
        }
    ]
}
